{
  "name": "css-puppet",
  "version": "1.0.0",
  "description": "Tool to remove unused CSS from website using Chrome and puppetter.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amit Kumar",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^1.20.0"
  }
}
